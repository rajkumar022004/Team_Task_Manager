# Local dev (Vite proxies /api to backend on port 5000)
VITE_API_URL=/api

# Production (set in Vercel) — must include /api suffix, no trailing slash
# VITE_API_URL=https://your-app.up.railway.app/api
